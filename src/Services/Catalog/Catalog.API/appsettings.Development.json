{
  //when Docker = debugger: set development env to run @ catalogdb:port, not localhost:port, 
  // because it points at catalogdb in docker-compose files
  "DatabaseSettings": {
    //"ConnectionString": "mongodb://catalogdb:27017",
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "ProductDb",
    "CollectionName": "Products"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
